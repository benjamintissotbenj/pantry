# Cloned upstream tooling (skills, etc.) — mirrored into .claude/skills/
clones/

# Gradle bootstrap (temporary, used to generate the wrapper)
gradle-bootstrap/
gradle-bootstrap.zip

# Android build outputs (created later by US-1)
.gradle/
build/
local.properties
.idea/
*.iml
captures/
.cxx/
.kotlin/
google-services.json
GoogleService-Info.plist
*.jks
*.keystore
upload-keystore.jks
keystore.properties

# Play store assets — screenshots upload directly, not committed
play-store/screenshots/

# Functions
functions/node_modules/
functions/lib/

# Firebase emulator logs and data
firebase-debug.log
firebase-debug.*.log
firestore-debug.log
ui-debug.log
.firebase/

# OS
.DS_Store
Thumbs.db

# Compiled scripts
scripts/*.class
